# WhatTodo PRD (Product Requirements Document)

> **버전**: 2.0
> **작성일**: 2025-12-29
> **핵심 원칙**: 오프라인 우선 | 시나리오 기반 | 자유로운 학습 | 영포자 친화

---

## Part 1: 제품 개요

### 1.1 한 줄 요약

**"30초만 있어도 영어 실력이 늘어나는, 영포자를 위한 맞춤형 학습 앱"**

### 1.2 핵심 차별점

| Duolingo | WhatTodo |
|----------|----------|
| 맥락 없는 고립된 문장 | **시나리오 기반** (카페, 공항 등) |
| 고정된 학습 시간 | **30초/1분/5분** 자유 선택 |
| 점수 = 압박감 | **"N개 배웠어요"** = 성취감 |
| 틀리면 창피 | **"80%가 헷갈려요"** = 안심 |
| 강제 순서 진행 | **자유로운 선택** |
| 온라인 필수 | **완전 오프라인** |
| Week/Level 잠금 | **목적별 경로** (여행/일/이민) |

### 1.3 핵심 원칙

#### 원칙 1: 오프라인 우선
```
✅ 오프라인으로 동작 (핵심 기능):
├── 모든 학습 콘텐츠 (로컬 JSON)
├── TTS 음성 재생 (expo-speech)
├── 진도/스킬 트래킹 (AsyncStorage)
├── SRS 복습 알고리즘
├── 일기 작성
├── 백업/복원 (파일 기반)
└── 모든 게이미피케이션

🟡 온라인 선택적 (프리미엄):
└── AI 회화 기능만 (OpenAI)
```

#### 원칙 2: 시나리오 기반 (Week 구조 없음)
```
❌ 기존: week-1, week-2... week-8 (강제 순서)
✅ 신규: 카페, 공항, 호텔, 식당... (자유 선택)

사용자가 원하는 시나리오를 선택
→ 해당 시나리오의 표현 학습
→ 스킬은 백엔드에서 자동 트래킹
```

#### 원칙 3: 자유로운 학습
```
- 순서 강제 없음
- 레벨 잠금 없음
- 목적에 맞는 시나리오 추천
- 사용자가 학습 경로 결정
```

#### 원칙 4: 영포자 친화적
```
- 앱 열기 자체가 부담 → 30초 모드
- 틀리면 창피 → "80%가 헷갈려요"
- 점수 압박 → "N개 배웠어요"
- 영어 용어 → 한국어로 설명
```

---

## Part 2: 타겟 사용자 (WHO)

### 2.1 Primary: 영포자 (English Quitters)

```yaml
프로필:
  나이: 20-40대
  특징: 영어 학습 여러 번 시도 후 포기
  인식: "영어 = 어렵다/창피하다"
  장벽: 앱 열기 자체가 부담
  집중력: 10분도 어려움

니즈:
  - 부담 없이 시작하고 싶다
  - 틀려도 창피하지 않았으면
  - 눈에 보이는 성장 확인
  - 작은 성공 경험

성공 기준:
  - "하루 1문장이라도 했다는 뿌듯함"
  - "앱 열기가 무섭지 않음"
```

### 2.2 Secondary: 바쁜 직장인

```yaml
프로필:
  나이: 25-45세
  특징: 학습 의지는 있으나 시간 부족
  가용 시간: 출퇴근, 점심시간만
  문제: 긴 세션 = 중단 = 허탈감

니즈:
  - 짧은 시간에 학습 완료
  - 끊겨도 이어할 수 있음
  - 실제 상황에서 쓸 수 있는 영어

성공 기준:
  - "출퇴근 시간에 꾸준히 학습"
  - "회사에서 간단한 영어 사용"
```

### 2.3 Tertiary: 목적 있는 학습자

```yaml
프로필:
  목적별:
    - 여행 준비 (3개월 후 유럽)
    - 이직/취업 (영어 면접)
    - 해외 이민 (일상 회화)

니즈:
  - 내 목적에 맞는 콘텐츠
  - 실용적인 표현
  - 빠른 실력 향상

성공 기준:
  - "현지에서 당당하게 주문"
  - "영어 면접 통과"
```

### 2.4 페르소나 상세

#### 최포기 (28세, 영포자)
```
영어 이력: 중학교 때 포기
두려움: "영어만 보면 머리가 하얘져요"
필요: "틀려도 안 무서운 앱"
WhatTodo 사용: 30초 모드로 하루 1문장
```

#### 김바쁨 (35세, 직장인)
```
영어 이력: 토익 700점대, 회화 못함
고민: "회사에서 영어 쓸 일 많아짐"
필요: "출퇴근에 할 수 있는 학습"
WhatTodo 사용: 1분 모드로 출근길 학습
```

#### 이여행 (26세, 여행 준비)
```
영어 이력: 기초 회화 가능
목표: "3개월 후 유럽 여행"
필요: "여행에서 쓸 수 있는 표현"
WhatTodo 사용: 여행 시나리오 집중 학습
```

#### 박어르신 (52세, 늦깎이)
```
영어 이력: 거의 없음
고민: "글씨가 너무 작아요"
필요: "쉬운 인터페이스"
WhatTodo 사용: 큰 폰트 + 느린 TTS
```

---

## Part 3: 핵심 기능 (WHAT)

### 3.1 온보딩

#### 3.1.1 목적 선택 (첫 화면)
```
┌─────────────────────────────────────┐
│  영어, 왜 배우시나요?                │
├─────────────────────────────────────┤
│                                     │
│  ✈️ 여행 가려고                     │
│     → 공항, 호텔, 식당 시나리오     │
│                                     │
│  💼 일/취업 때문에                  │
│     → 비즈니스, 면접 시나리오       │
│                                     │
│  🌏 해외 살 예정                    │
│     → 일상, 은행, 병원 시나리오     │
│                                     │
│  📚 그냥 공부하고 싶어              │
│     → 전체 시나리오 열림            │
│                                     │
└─────────────────────────────────────┘

→ 선택에 따라 홈 화면 시나리오 추천 순서 변경
→ 나중에 설정에서 변경 가능
```

#### 3.1.2 난이도 선택 (두 번째 화면)
```
┌─────────────────────────────────────┐
│  영어 실력이 어느 정도예요?          │
├─────────────────────────────────────┤
│                                     │
│  🌱 처음이에요                      │
│     "Hello도 떨려요"                │
│     → 기초 시나리오 + 쉬운 표현     │
│                                     │
│  🌿 조금 해요                       │
│     "간단한 인사는 가능"            │
│     → 일반 시나리오                 │
│                                     │
│  🌳 중급이에요                      │
│     "일상 대화는 되는데 막힘"       │
│     → 심화 표현 + 뉘앙스            │
│                                     │
│  [스킵 - 나중에 정할게요]           │
│                                     │
└─────────────────────────────────────┘

→ 레벨 테스트 없이 자기 선택
→ 언제든 변경 가능
→ 스킵하면 기본(조금 해요)으로 시작
```

### 3.2 홈 화면

#### 3.2.1 시나리오 카드
```
┌─────────────────────────────────────┐
│  오늘 뭐 배울까요?                   │
├─────────────────────────────────────┤
│                                     │
│  🔥 추천 시나리오                    │
│  ┌──────────────────────────────┐  │
│  │ ☕ 카페에서 주문하기           │  │
│  │ "Can I get a latte?"         │  │
│  │ 🎯 10개 표현 · ⏱️ 5분         │  │
│  └──────────────────────────────┘  │
│                                     │
│  📂 전체 시나리오                    │
│  ┌────────┐ ┌────────┐            │
│  │ ✈️ 공항 │ │ 🏨 호텔 │            │
│  └────────┘ └────────┘            │
│  ┌────────┐ ┌────────┐            │
│  │ 🍽️ 식당 │ │ 🛒 쇼핑 │            │
│  └────────┘ └────────┘            │
│                                     │
│  📊 내 진행 상황                     │
│  ├── 배운 표현: 47개               │
│  ├── 복습 필요: 12개               │
│  └── 🔥 3일 연속!                  │
│                                     │
└─────────────────────────────────────┘
```

#### 3.2.2 목적별 필터
```
사용자 목적에 따라 시나리오 순서 변경:

여행 선택 시:
1. 공항 체크인
2. 입국 심사
3. 호텔 체크인
4. 식당 주문
5. 카페 주문
...

비즈니스 선택 시:
1. 자기소개
2. 미팅
3. 이메일
4. 전화
5. 프레젠테이션
...
```

### 3.3 세션 시스템

#### 3.3.1 시간 선택
```
┌─────────────────────────────────────┐
│  오늘 얼마나 할 수 있어요?          │
├─────────────────────────────────────┤
│                                     │
│  ⚡ 30초                            │
│     문장 1개만                      │
│     "진짜 바쁠 때"                  │
│                                     │
│  🔥 1분                             │
│     문장 3개                        │
│     "잠깐 시간 날 때"               │
│                                     │
│  💪 5분                             │
│     문장 10개                       │
│     "제대로 공부할 때"              │
│                                     │
└─────────────────────────────────────┘

핵심:
- 30초도 완료 = 스트릭 유지 = 성공 경험
- 중간에 끊겨도 한 문장이라도 저장
- "시간 없어서 못 함" 변명 제거
```

#### 3.3.2 세션 구조: 강점 → 약점
```
5분 세션 예시 (10문장):

[1-3] 강점 (30%) - 자신감 빌드업
├── 이미 아는 표현 복습
├── 쉬운 문장으로 워밍업
└── "나 할 수 있네!" 느낌

[4-8] 약점 (50%) - 실력 향상
├── 자주 틀리는 유형
├── 새로운 표현
└── 도전적이지만 가능한 수준

[9-10] 확장 (20%) - 응용
├── 배운 표현 변형
├── 비슷한 상황 적용
└── 전이 학습 효과
```

#### 3.3.3 중단 시 처리
```
세션 중 앱 종료/전화 옴:

✅ 완료한 문장은 자동 저장
✅ "1개 배웠어요!" 인정
✅ 스트릭 유지 (1개라도 하면)
✅ 나중에 이어서 가능
```

### 3.4 학습 화면

#### 3.4.1 문장 학습 UI
```
┌─────────────────────────────────────┐
│  ☕ 카페에서                 [2/10] │
├─────────────────────────────────────┤
│                                     │
│  💬 이걸 영어로?                    │
│                                     │
│  "라떼 한 잔 주세요"                │
│                                     │
│  ─────────────────────────────────  │
│                                     │
│  [          입력창          ]      │
│                                     │
│  💡 힌트: Can I get...             │
│                                     │
│  🔊 [듣기]                         │
│                                     │
└─────────────────────────────────────┘
```

#### 3.4.2 피드백 시스템

**정답일 때:**
```
┌─────────────────────────────────────┐
│  🎉 완벽해요!                       │
├─────────────────────────────────────┤
│                                     │
│  ✅ "Can I get a latte, please?"   │
│                                     │
│  💡 추가 팁:                        │
│  "Can I get..."은 주문할 때        │
│  가장 자연스러운 표현이에요!        │
│                                     │
│  [다음 문장]                        │
│                                     │
└─────────────────────────────────────┘
```

**오답일 때:**
```
┌─────────────────────────────────────┐
│  😊 아깝다!                         │
├─────────────────────────────────────┤
│                                     │
│  ❌ 내 답: "Give me a latte"        │
│  ✅ 정답: "Can I get a latte?"     │
│                                     │
│  💡 왜?                             │
│  "Give me"는 너무 직접적이에요.     │
│  "Can I get..."이 더 공손해요.     │
│                                     │
│  📊 이 표현은 72%가 헷갈려해요      │
│     (혼자가 아니에요!)              │
│                                     │
│  [다시 해볼래요] [다음으로]         │
│                                     │
└─────────────────────────────────────┘

핵심:
- "틀렸습니다" ❌ → "아깝다!" ✅
- 에러 정규화: "N%가 헷갈려요"
- 짧은 한국어 설명
- 재도전 옵션 제공
```

#### 3.4.3 완료 화면
```
┌─────────────────────────────────────┐
│  🎉 오늘도 해냈어요!                 │
├─────────────────────────────────────┤
│                                     │
│  📚 오늘 배운 것: 5개               │
│                                     │
│  💪 잘한 것:                        │
│     • "Can I get..." 패턴 완벽!     │
│     • 카페 주문 표현 3개 마스터     │
│                                     │
│  📝 복습하면 좋을 것:               │
│     • 관사 (a/the) 사용             │
│     → 내일 다시 나올 거예요!        │
│                                     │
│  ─────────────────────────────────  │
│                                     │
│  🔥 3일 연속 학습 중!               │
│  ⭐ +15 별 획득!                    │
│                                     │
│  [홈으로] [오늘 배운 거 일기에 쓰기]│
│                                     │
└─────────────────────────────────────┘

핵심:
- 점수 없음 → "N개 배웠어요"
- 잘한 것 먼저 (긍정 강화)
- 약점은 부드럽게 (내일 다시)
- 일기 연결 유도
```

### 3.5 일기 연동

#### 3.5.1 오늘 배운 표현 표시
```
┌─────────────────────────────────────┐
│  ✍️ 오늘의 영어 일기                 │
├─────────────────────────────────────┤
│                                     │
│  💡 오늘 배운 표현 (탭해서 삽입):   │
│  ┌──────────────────────────────┐  │
│  │ Can I get...                 │  │
│  │ I'd like to...               │  │
│  │ Could you please...          │  │
│  └──────────────────────────────┘  │
│                                     │
│  ─────────────────────────────────  │
│                                     │
│  [일기 작성 영역]                   │
│                                     │
│  오늘 카페에서 커피를 샀다.         │
│  "Can I get a latte?" 라고 말했다. │
│                                     │
│  ─────────────────────────────────  │
│                                     │
│  📝 예문 도우미:                    │
│  ┌──────────────────────────────┐  │
│  │ "Today I went to a cafe."    │  │
│  │ "I ordered a latte."         │  │
│  │ "It was delicious!"          │  │
│  └──────────────────────────────┘  │
│                                     │
└─────────────────────────────────────┘

핵심:
- 배운 표현 → 바로 사용
- 예문 템플릿 제공 (API 없이!)
- 탭하면 자동 삽입
```

#### 3.5.2 예문 템플릿 (오프라인)
```
시나리오별 일기 예문 템플릿:

[카페]
- "Today I went to a cafe."
- "I ordered ___."
- "It tasted ___."

[여행]
- "Today I arrived at ___."
- "I checked into ___."
- "The weather was ___."

[일상]
- "Today I met ___."
- "We talked about ___."
- "It was fun/interesting/boring."

→ API 없이 로컬 템플릿으로 제공
→ 사용자가 빈칸만 채우면 됨
```

### 3.6 기존 기능 통합

#### 3.6.1 Todo → 별 → 학습
```
기존 WhatTodo 핵심 메커니즘:

Todo 완료 → ⭐ 별 획득 → 학습 잠금해제

연결 방식:
1. Todo 완료 시 별 지급 (기존 유지)
2. 별로 특별 시나리오 해금
3. 별로 힌트/스킵 구매
4. 일일 무료 학습 + 별로 추가 학습
```

#### 3.6.2 보상 시스템 연결
```
기존 rewardStore 활용:

⭐ 별 사용처:
├── 힌트 사용 (10⭐)
├── 문장 스킵 (20⭐)
├── 특별 시나리오 해금 (100⭐)
├── 스트릭 프리즈 (50⭐)
└── 테마 구매 (200⭐)

🔥 스트릭 시스템 (기존 유지):
├── 일일 학습 완료 시 유지
├── 30초 모드도 인정
├── 프리즈로 1일 보호
```

#### 3.6.3 일기 시스템 연결
```
기존 diaryStore 활용:

학습 완료 → 일기 화면 유도
├── "오늘 배운 거 일기에 써볼까?"
├── 배운 표현 자동 표시
├── 예문 템플릿 제공
└── 일기 작성 시 보너스 별
```

---

## Part 4: 콘텐츠 구조 (데이터)

### 4.1 시나리오 목록

> **핵심**: Week 구조 없음! 시나리오 기반으로만 구성

#### 4.1.1 여행 시나리오 (Travel)
```yaml
공항:
  - 공항 체크인 (Airport Check-in)
  - 입국 심사 (Immigration)
  - 수하물 찾기 (Baggage Claim)
  - 환승 (Transfer)

숙소:
  - 호텔 체크인 (Hotel Check-in)
  - 룸서비스 (Room Service)
  - 호텔 체크아웃 (Hotel Check-out)
  - 에어비앤비 (Airbnb)

식사:
  - 카페 주문 (Cafe Order)
  - 식당 주문 (Restaurant Order)
  - 패스트푸드 (Fast Food)
  - 술집/바 (Bar)

교통:
  - 택시 (Taxi)
  - 버스 (Bus)
  - 기차 (Train)
  - 렌터카 (Car Rental)

쇼핑:
  - 옷 가게 (Clothing Store)
  - 마트 (Grocery)
  - 기념품 (Souvenirs)
  - 환불/교환 (Return/Exchange)

긴급:
  - 병원 (Hospital)
  - 약국 (Pharmacy)
  - 분실물 (Lost & Found)
  - 경찰서 (Police)
```

#### 4.1.2 비즈니스 시나리오 (Business)
```yaml
일상:
  - 자기소개 (Self Introduction)
  - 전화 응대 (Phone Call)
  - 이메일 작성 (Email Writing)
  - 미팅 (Meeting)

면접:
  - 면접 인사 (Interview Greeting)
  - 경력 설명 (Experience Description)
  - 질문하기 (Asking Questions)
  - 연봉 협상 (Salary Negotiation)

프레젠테이션:
  - 시작하기 (Opening)
  - 본론 (Main Points)
  - 마무리 (Closing)
  - Q&A (Q&A Session)

협업:
  - 요청하기 (Making Requests)
  - 거절하기 (Declining Politely)
  - 피드백 주기 (Giving Feedback)
  - 사과하기 (Apologizing)
```

#### 4.1.3 일상 시나리오 (Daily)
```yaml
기초:
  - 인사하기 (Greetings)
  - 날씨 이야기 (Weather Talk)
  - 자기소개 (Self Introduction)
  - 시간/날짜 (Time & Date)

사교:
  - 처음 만났을 때 (First Meeting)
  - 취미 이야기 (Hobbies)
  - 가족 소개 (Family)
  - 약속 잡기 (Making Plans)

생활:
  - 은행 (Bank)
  - 우체국 (Post Office)
  - 세탁소 (Laundry)
  - 미용실 (Hair Salon)

긴급:
  - 119 전화 (Emergency Call)
  - 증상 설명 (Describing Symptoms)
  - 길 찾기 (Asking for Directions)
  - 도움 요청 (Asking for Help)
```

### 4.2 스킬 분류 (백엔드 자동 트래킹)

> **핵심**: 사용자는 시나리오만 보지만, 백엔드에서 스킬 자동 추적

#### 4.2.1 스킬 카테고리
```typescript
// 6가지 스킬 카테고리
type SkillCategory =
  | 'vocabulary'  // 어휘
  | 'grammar'     // 문법
  | 'listening'   // 듣기
  | 'speaking'    // 말하기
  | 'reading'     // 읽기
  | 'writing';    // 쓰기
```

#### 4.2.2 세부 스킬 태그
```yaml
vocabulary:
  - vocab.greetings      # 인사 어휘
  - vocab.food           # 음식 어휘
  - vocab.travel         # 여행 어휘
  - vocab.business       # 비즈니스 어휘
  - vocab.emotions       # 감정 어휘
  - vocab.numbers        # 숫자
  - vocab.time           # 시간
  ...

grammar:
  - grammar.article      # 관사 (a/an/the)
  - grammar.tense.present    # 현재 시제
  - grammar.tense.past       # 과거 시제
  - grammar.tense.future     # 미래 시제
  - grammar.modal        # 조동사 (can, could, would)
  - grammar.preposition  # 전치사
  - grammar.question     # 의문문
  - grammar.negative     # 부정문
  ...

patterns:
  - pattern.request      # 요청하기 (Can I get...?)
  - pattern.permission   # 허락 구하기 (May I...?)
  - pattern.suggestion   # 제안하기 (How about...?)
  - pattern.comparison   # 비교하기 (bigger than...)
  - pattern.preference   # 선호 표현 (I'd rather...)
  ...
```

#### 4.2.3 스킬 트래킹 로직
```
사용자 행동:
"카페에서 주문하기" 시나리오 학습

→ 문장: "Can I get a latte, please?"

백엔드 자동 트래킹:
├── vocab.food (latte)
├── grammar.article (a)
├── grammar.modal (can)
├── pattern.request (Can I get...?)
└── 시나리오: cafe.order

→ 사용자에게 보이는 것: "카페 주문 표현 1개 배움!"
→ 내부 기록: 4개 스킬 포인트 획득
```

### 4.3 데이터 스키마

#### 4.3.1 시나리오 (Scenario)
```typescript
interface Scenario {
  id: string;                    // "cafe-order"
  category: 'travel' | 'business' | 'daily';
  name: string;                  // "카페에서 주문하기"
  nameEn: string;                // "Cafe Order"
  description: string;           // "커피숍에서 주문하는 표현"
  icon: string;                  // "☕"
  difficulty: 'beginner' | 'intermediate' | 'advanced';
  estimatedTime: number;         // 5 (분)
  expressionCount: number;       // 10 (표현 개수)
  tags: string[];                // ["food", "ordering", "polite"]
}
```

#### 4.3.2 표현 (Expression)
```typescript
interface Expression {
  id: string;                    // "cafe-order-1"
  scenarioId: string;            // "cafe-order"

  // 핵심 콘텐츠
  korean: string;                // "라떼 한 잔 주세요"
  english: string;               // "Can I get a latte, please?"
  pronunciation?: string;        // "캔 아이 겟 어 라떼, 플리즈?"

  // 학습 보조
  hint: string;                  // "Can I get..."
  explanation: string;           // "'Can I get'은 주문할 때 자연스러운 표현"
  alternatives?: string[];       // ["I'd like a latte", "A latte, please"]

  // 스킬 태그 (자동 트래킹용)
  skills: string[];              // ["grammar.modal", "pattern.request", "vocab.food"]

  // 난이도
  difficulty: 1 | 2 | 3;         // 1: 쉬움, 2: 보통, 3: 어려움

  // 에러 정규화 데이터
  commonMistakes?: {
    wrong: string;               // "Give me a latte"
    explanation: string;         // "Give me는 너무 직접적"
    percentage: number;          // 72 (72%가 이렇게 틀림)
  }[];
}
```

#### 4.3.3 사용자 스킬 상태 (SkillState)
```typescript
interface SkillState {
  skillId: string;               // "grammar.article"

  // SRS 데이터
  level: number;                 // 0-5 (SM-2 레벨)
  easeFactor: number;            // 2.5 (기본)
  interval: number;              // 다음 복습까지 일수
  nextReview: Date;              // 다음 복습 날짜

  // 통계
  totalAttempts: number;         // 총 시도 횟수
  correctAttempts: number;       // 정답 횟수
  lastPracticed: Date;           // 마지막 연습 시간

  // 트렌드
  recentAccuracy: number[];      // 최근 10회 정답률 [1,1,0,1,1,0,1,1,1,1]
  streak: number;                // 연속 정답
}
```

#### 4.3.4 학습 세션 (LearningSession)
```typescript
interface LearningSession {
  id: string;
  userId: string;
  startedAt: Date;
  endedAt?: Date;

  // 세션 설정
  mode: '30sec' | '1min' | '5min';
  scenarioId: string;

  // 결과
  expressionsAttempted: number;
  expressionsCorrect: number;
  expressionsLearned: string[];  // expression IDs
  skillsImproved: string[];      // skill IDs

  // 중단 복구용
  currentIndex: number;
  isCompleted: boolean;
}
```

### 4.4 콘텐츠 파일 구조

#### 4.4.1 새로운 폴더 구조
```
data/
├── scenarios/
│   ├── travel/
│   │   ├── cafe-order.json
│   │   ├── airport-checkin.json
│   │   ├── hotel-checkin.json
│   │   └── ...
│   ├── business/
│   │   ├── self-introduction.json
│   │   ├── meeting.json
│   │   └── ...
│   └── daily/
│       ├── greetings.json
│       ├── weather.json
│       └── ...
├── templates/
│   └── diary-templates.json    # 일기 예문 템플릿
└── skills/
    └── skill-definitions.json  # 스킬 정의
```

#### 4.4.2 시나리오 파일 예시 (cafe-order.json)
```json
{
  "id": "cafe-order",
  "category": "travel",
  "name": "카페에서 주문하기",
  "nameEn": "Cafe Order",
  "description": "커피숍에서 음료를 주문하는 표현을 배워요",
  "icon": "☕",
  "difficulty": "beginner",
  "estimatedTime": 5,
  "expressions": [
    {
      "id": "cafe-order-1",
      "korean": "라떼 한 잔 주세요",
      "english": "Can I get a latte, please?",
      "pronunciation": "캔 아이 겟 어 라떼, 플리즈?",
      "hint": "Can I get...",
      "explanation": "'Can I get...'은 주문할 때 가장 자연스러운 표현이에요.",
      "alternatives": ["I'd like a latte, please", "A latte, please"],
      "skills": ["grammar.modal", "pattern.request", "vocab.food"],
      "difficulty": 1,
      "commonMistakes": [
        {
          "wrong": "Give me a latte",
          "explanation": "'Give me'는 너무 직접적이에요. 'Can I get'이 더 공손해요.",
          "percentage": 72
        }
      ]
    },
    {
      "id": "cafe-order-2",
      "korean": "여기서 마실게요",
      "english": "For here, please",
      "hint": "For here...",
      "explanation": "매장에서 마실 때 사용해요. 반대로 'To go'는 포장할 때.",
      "skills": ["vocab.food", "pattern.preference"],
      "difficulty": 1
    }
  ]
}
```

### 4.5 콘텐츠 생성 가이드

#### 4.5.1 표현 작성 원칙
```yaml
1. 실용성 우선:
   - 실제로 쓰이는 표현만
   - 교과서 영어 X, 현지 영어 O
   - 자연스러운 구어체

2. 난이도 분배:
   - 시나리오당 10개 표현 권장
   - 쉬움 4개 / 보통 4개 / 어려움 2개

3. 스킬 태깅:
   - 각 표현에 2-4개 스킬 태그
   - 주요 문법 포인트 필수 포함
   - 어휘 태그 포함

4. 에러 데이터:
   - 흔한 실수 1-2개 포함
   - 퍼센트는 예상 수치 (나중에 실데이터로 교체)

5. 한국어 설명:
   - 짧고 친근하게
   - 문법 용어 최소화
   - 비유나 예시 활용
```

#### 4.5.2 마이그레이션: 기존 week 데이터 → 시나리오
```
기존 데이터 활용:
data/activities/a1/vocabulary/week-1-vocabulary.json
→ 어휘를 시나리오별로 재분류

예시:
week-1의 "hello", "thank you"
→ scenarios/daily/greetings.json으로 이동

week-3의 "coffee", "latte"
→ scenarios/travel/cafe-order.json으로 이동

마이그레이션 스크립트로 자동화 가능
```

---

## Part 5: 기술 구현 (코드)

### 5.1 아키텍처 원칙

#### 5.1.1 오프라인/온라인 분리
```
┌──────────────────────────────────────────────────┐
│                    WhatTodo App                   │
├──────────────────────────────────────────────────┤
│                                                   │
│  ┌─────────────────────────────────────────────┐ │
│  │         오프라인 레이어 (핵심)               │ │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐       │ │
│  │  │ 시나리오 │ │  학습   │ │ 진도/SRS │       │ │
│  │  │  JSON   │ │  화면   │ │ 트래킹  │       │ │
│  │  └─────────┘ └─────────┘ └─────────┘       │ │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐       │ │
│  │  │   TTS   │ │  일기   │ │  보상   │       │ │
│  │  │(expo-   │ │ 템플릿  │ │ 시스템  │       │ │
│  │  │speech)  │ │        │ │        │       │ │
│  │  └─────────┘ └─────────┘ └─────────┘       │ │
│  │                                             │ │
│  │  저장소: AsyncStorage (로컬)                │ │
│  └─────────────────────────────────────────────┘ │
│                                                   │
│  ┌─────────────────────────────────────────────┐ │
│  │         온라인 레이어 (선택적/프리미엄)       │ │
│  │  ┌─────────────────────────────────────┐   │ │
│  │  │              AI 회화                 │   │ │
│  │  │         (OpenAI API)                │   │ │
│  │  └─────────────────────────────────────┘   │ │
│  │                                             │ │
│  │  ⚠️ 오프라인 시: "AI 회화는 인터넷 필요"    │ │
│  │     → 나머지 기능은 정상 작동               │ │
│  └─────────────────────────────────────────────┘ │
│                                                   │
└──────────────────────────────────────────────────┘
```

#### 5.1.2 기술 스택
```yaml
프레임워크:
  - Expo SDK 51
  - React Native 0.74.5
  - TypeScript 5.9

상태 관리:
  - Zustand 5.0 (persist middleware)
  - AsyncStorage (로컬 저장)

UI:
  - React Native Paper 5.14
  - React Native Reanimated 3.10
  - Expo Router 3.5

오디오:
  - expo-speech (TTS, 오프라인)

AI (선택적):
  - OpenAI API (gpt-3.5-turbo)
```

### 5.2 Store 구조

#### 5.2.1 신규/수정할 Store
```typescript
// store/scenarioStore.ts (신규)
interface ScenarioStore {
  // 시나리오 목록
  scenarios: Scenario[];

  // 현재 학습 중인 시나리오
  currentScenario: string | null;

  // 시나리오별 진행률
  progress: Record<string, {
    completed: number;
    total: number;
    lastAccess: Date;
  }>;

  // 액션
  loadScenarios: () => void;
  getScenario: (id: string) => Scenario | null;
  updateProgress: (scenarioId: string, expressionId: string) => void;
}
```

```typescript
// store/skillStore.ts (신규)
interface SkillStore {
  // 스킬별 상태
  skills: Record<string, SkillState>;

  // 약점/강점 분석
  getWeakSkills: (count: number) => SkillState[];
  getStrongSkills: (count: number) => SkillState[];

  // SRS 업데이트
  updateSkill: (skillId: string, correct: boolean) => void;
  getReviewDue: () => string[]; // 복습 필요한 스킬
}
```

```typescript
// store/sessionStore.ts (수정)
interface SessionStore {
  // 현재 세션
  currentSession: LearningSession | null;

  // 세션 시작/종료
  startSession: (mode: '30sec' | '1min' | '5min', scenarioId: string) => void;
  endSession: () => void;

  // 중단 복구
  resumeSession: () => LearningSession | null;

  // 표현 결과 기록
  recordAttempt: (expressionId: string, correct: boolean) => void;
}
```

#### 5.2.2 기존 Store 활용
```typescript
// 수정 없이 그대로 사용
- taskStore.ts      // Todo 관리
- rewardStore.ts    // 별/스트릭/배지
- diaryStore.ts     // 일기

// 약간 수정
- learnStore.ts     // 시나리오 기반으로 변경
- srsStore.ts       // skillStore와 통합 검토
```

### 5.3 컴포넌트 구조

#### 5.3.1 신규 컴포넌트
```
components/
├── scenarios/
│   ├── ScenarioCard.tsx        # 시나리오 카드
│   ├── ScenarioList.tsx        # 시나리오 목록
│   ├── ScenarioDetail.tsx      # 시나리오 상세
│   └── ScenarioProgress.tsx    # 진행률 표시
│
├── session/
│   ├── SessionSelector.tsx     # 30초/1분/5분 선택
│   ├── SessionTimer.tsx        # 타이머 표시
│   ├── SessionProgress.tsx     # 세션 진행률
│   └── SessionComplete.tsx     # 완료 화면
│
├── expression/
│   ├── ExpressionCard.tsx      # 표현 학습 카드
│   ├── ExpressionInput.tsx     # 입력 영역
│   ├── ExpressionFeedback.tsx  # 피드백 (정답/오답)
│   └── ExpressionHint.tsx      # 힌트 표시
│
└── skills/
    ├── SkillBadge.tsx          # 스킬 뱃지
    ├── SkillProgress.tsx       # 스킬 진행률
    └── WeaknessCard.tsx        # 약점 표시 카드
```

#### 5.3.2 수정할 컴포넌트
```
components/home/
├── TodayScreen.tsx      # 시나리오 카드로 변경
├── QuickActions.tsx     # 세션 선택 추가
└── StatsCard.tsx        # 스킬 진행률 추가

components/learn/
├── LearnScreen.tsx      # 세션 기반으로 변경
└── QuizCard.tsx         # ExpressionCard로 교체
```

### 5.4 화면 구조

#### 5.4.1 수정할 화면
```
app/
├── (tabs)/
│   ├── index.tsx        # 시나리오 기반 홈
│   └── learn.tsx        # 세션 선택 → 학습
│
├── scenario/
│   ├── [id].tsx         # 시나리오 상세 (신규)
│   └── session.tsx      # 학습 세션 (신규)
│
└── learn/
    └── [type].tsx       # 기존 유지 (SRS 복습용)
```

### 5.5 데이터 마이그레이션

#### 5.5.1 기존 데이터 → 신규 구조
```typescript
// utils/migration.ts

/**
 * week 기반 데이터를 시나리오 기반으로 변환
 */
export async function migrateToScenarios() {
  // 1. 기존 진행률 로드
  const oldProgress = await loadOldProgress();

  // 2. 표현 매핑 테이블 로드
  // (기존 week-N 표현 → 새 시나리오 매핑)
  const mappingTable = await loadMappingTable();

  // 3. 새 구조로 변환
  const newProgress = convertProgress(oldProgress, mappingTable);

  // 4. 저장
  await saveNewProgress(newProgress);

  // 5. 마이그레이션 완료 플래그
  await markMigrationComplete();
}

/**
 * 앱 시작 시 마이그레이션 체크
 */
export async function checkMigration() {
  const migrated = await AsyncStorage.getItem('migration_v2_complete');
  if (!migrated) {
    await migrateToScenarios();
  }
}
```

#### 5.5.2 마이그레이션 안전장치
```typescript
// 마이그레이션 전 백업
export async function backupBeforeMigration() {
  const allData = await AsyncStorage.getAllKeys();
  const backup = {};
  for (const key of allData) {
    backup[key] = await AsyncStorage.getItem(key);
  }
  await AsyncStorage.setItem('backup_pre_v2', JSON.stringify(backup));
}

// 롤백 기능
export async function rollbackMigration() {
  const backup = await AsyncStorage.getItem('backup_pre_v2');
  if (backup) {
    const data = JSON.parse(backup);
    for (const [key, value] of Object.entries(data)) {
      await AsyncStorage.setItem(key, value as string);
    }
  }
}
```

### 5.6 수정할 파일 목록

#### 5.6.1 신규 파일
```yaml
Store:
  - store/scenarioStore.ts
  - store/skillStore.ts

컴포넌트:
  - components/scenarios/ScenarioCard.tsx
  - components/scenarios/ScenarioList.tsx
  - components/session/SessionSelector.tsx
  - components/session/SessionTimer.tsx
  - components/expression/ExpressionCard.tsx
  - components/expression/ExpressionFeedback.tsx

화면:
  - app/scenario/[id].tsx
  - app/scenario/session.tsx

데이터:
  - data/scenarios/travel/cafe-order.json
  - data/scenarios/... (시나리오별)

유틸:
  - utils/migration.ts
  - utils/skillTracking.ts
```

#### 5.6.2 수정 파일
```yaml
Store:
  - store/learnStore.ts     # 시나리오 기반 진행률
  - store/sessionStore.ts   # 세션 시스템 확장

컴포넌트:
  - components/home/TodayScreen.tsx    # 시나리오 카드
  - components/learn/LearnScreen.tsx   # 세션 기반

화면:
  - app/(tabs)/index.tsx    # 홈 화면 개편
  - app/(tabs)/learn.tsx    # 학습 탭 개편
```

#### 5.6.3 삭제/교체 파일
```yaml
교체:
  - components/learn/QuizCard.tsx → ExpressionCard.tsx
  - components/learn/ActivityCard.tsx → ScenarioCard.tsx

폴더 이동:
  - data/activities/** → data/scenarios/** (구조 변경)
```

### 5.7 API 인터페이스 (AI 회화용)

#### 5.7.1 AI 서비스 구조
```typescript
// services/aiTutor.ts (기존 파일 유지)

// 온라인 상태 체크
export function isOnline(): boolean {
  return NetInfo.fetch().then(state => state.isConnected);
}

// AI 사용 전 체크
export async function canUseAI(): Promise<{
  available: boolean;
  reason?: string;
}> {
  if (!isOnline()) {
    return { available: false, reason: '인터넷 연결이 필요해요' };
  }

  const { canUse, remaining } = checkUsageLimit();
  if (!canUse) {
    return { available: false, reason: '오늘 무료 사용 횟수를 다 썼어요' };
  }

  return { available: true };
}
```

#### 5.7.2 오프라인 폴백
```typescript
// AI 불가능 시 대체 기능
export function getOfflineAlternative(feature: string): string {
  switch (feature) {
    case 'grammar_check':
      return '문법 검사는 인터넷이 필요해요. 대신 예문을 참고해보세요!';
    case 'conversation':
      return 'AI 대화는 인터넷이 필요해요. 대신 시나리오 연습을 해볼까요?';
    default:
      return '이 기능은 인터넷이 필요해요.';
  }
}
```

---

## Part 5.5: 디자인 시스템 & 프론트엔드

### 5.8 디자인 시스템

#### 5.8.1 컬러 팔레트
```yaml
Primary Colors:
  primary: "#2196F3"         # 메인 블루
  primaryLight: "#64B5F6"    # 라이트 블루
  primaryDark: "#1976D2"     # 다크 블루

Secondary Colors:
  secondary: "#FF9800"       # 오렌지 (강조)
  success: "#4CAF50"         # 그린 (정답)
  error: "#F44336"           # 레드 (오답) - 부드럽게 사용
  warning: "#FFC107"         # 옐로우 (주의)

Neutral Colors:
  background: "#FAFAFA"      # 배경
  surface: "#FFFFFF"         # 카드 배경
  textPrimary: "#212121"     # 주 텍스트
  textSecondary: "#757575"   # 보조 텍스트
  border: "#E0E0E0"          # 테두리
  disabled: "#BDBDBD"        # 비활성

Semantic Colors:
  streak: "#FF5722"          # 스트릭 오렌지
  star: "#FFD700"            # 별 골드
  correct: "#81C784"         # 정답 (부드러운 그린)
  incorrect: "#FFAB91"       # 오답 (부드러운 코랄) ← 빨강 아님!
  hint: "#CE93D8"            # 힌트 퍼플

Dark Mode:
  background: "#121212"
  surface: "#1E1E1E"
  textPrimary: "#FFFFFF"
  textSecondary: "#B0B0B0"
  primary: "#64B5F6"         # 밝은 블루
```

#### 5.8.2 타이포그래피
```yaml
Font Family:
  기본: System Font (San Francisco / Roboto)
  영어: "Pretendard" 또는 System
  숫자: "Roboto Mono" (통계용)

Font Sizes:
  xs: 12px    # 캡션, 힌트
  sm: 14px    # 보조 텍스트
  md: 16px    # 기본 텍스트
  lg: 18px    # 강조 텍스트
  xl: 20px    # 서브 타이틀
  xxl: 24px   # 타이틀
  xxxl: 32px  # 대형 타이틀

Font Weights:
  regular: 400
  medium: 500
  semibold: 600
  bold: 700

Line Heights:
  tight: 1.2   # 타이틀
  normal: 1.5  # 본문
  relaxed: 1.8 # 학습 콘텐츠 (읽기 편하게)

영어 학습 특화:
  englishText: 20px, weight: 500, lineHeight: 1.6
  koreanText: 16px, weight: 400, lineHeight: 1.5
  pronunciationText: 14px, weight: 400, color: textSecondary
```

#### 5.8.3 간격 시스템 (Spacing)
```yaml
Base: 4px

Spacing Scale:
  xs: 4px      # 아이콘-텍스트 간격
  sm: 8px      # 요소 내부 간격
  md: 16px     # 카드 패딩
  lg: 24px     # 섹션 간격
  xl: 32px     # 영역 간격
  xxl: 48px    # 화면 상하 여백

Component Specific:
  cardPadding: 16px
  cardMargin: 12px
  buttonPadding: "12px 24px"
  inputPadding: "14px 16px"
  listItemPadding: "16px"
  screenPadding: "16px"     # 화면 좌우 여백
```

#### 5.8.4 Border Radius
```yaml
none: 0
sm: 4px      # 작은 버튼, 태그
md: 8px      # 일반 카드
lg: 12px     # 큰 카드, 모달
xl: 16px     # 하단 시트
full: 9999px # 원형 버튼, 아바타
```

#### 5.8.5 그림자 (Elevation)
```yaml
none: "none"
sm: "0 1px 2px rgba(0,0,0,0.1)"      # 카드 기본
md: "0 2px 8px rgba(0,0,0,0.12)"     # 떠있는 카드
lg: "0 4px 16px rgba(0,0,0,0.15)"    # 모달
xl: "0 8px 32px rgba(0,0,0,0.2)"     # 바텀시트
```

#### 5.8.6 아이콘 시스템
```yaml
Library: "@expo/vector-icons" (MaterialCommunityIcons)

Sizes:
  xs: 16px   # 인라인 아이콘
  sm: 20px   # 리스트 아이콘
  md: 24px   # 기본 아이콘
  lg: 32px   # 탭바 아이콘
  xl: 48px   # 빈 상태 아이콘
  xxl: 64px  # 온보딩 아이콘

시나리오 이모지:
  travel:
    airport: "✈️"
    hotel: "🏨"
    cafe: "☕"
    restaurant: "🍽️"
    shopping: "🛒"
    taxi: "🚕"
  business:
    meeting: "💼"
    email: "📧"
    phone: "📞"
    presentation: "📊"
  daily:
    greetings: "👋"
    weather: "🌤️"
    family: "👨‍👩‍👧"
    hobby: "🎮"

상태 아이콘:
  correct: "✓" (체크마크, 초록)
  incorrect: "✗" (엑스, 주황) ← 빨강 아님
  streak: "🔥"
  star: "⭐"
  hint: "💡"
  audio: "🔊"
```

### 5.9 컴포넌트 스타일 가이드

#### 5.9.1 버튼 (Buttons)
```
Primary Button:
┌─────────────────────────────────────┐
│          다음으로                    │
└─────────────────────────────────────┘
- 배경: primary (#2196F3)
- 텍스트: white, 16px, semibold
- 높이: 48px
- borderRadius: 12px
- 전체 너비 (학습 화면)

Secondary Button:
┌─────────────────────────────────────┐
│          건너뛰기                    │
└─────────────────────────────────────┘
- 배경: transparent
- 테두리: primary, 1.5px
- 텍스트: primary, 16px, medium
- 높이: 48px

Ghost Button:
- 배경: transparent
- 텍스트: textSecondary
- 높이: 40px
- 호버 시: background rgba(0,0,0,0.05)

Disabled State (모든 버튼):
- 배경: disabled (#BDBDBD)
- 텍스트: white
- opacity: 0.6
```

#### 5.9.2 카드 (Cards)
```
Scenario Card:
┌─────────────────────────────────────┐
│  ☕                                 │
│  카페에서 주문하기                   │
│  "Can I get a latte?"              │
│  ─────────────────────────────────  │
│  🎯 10개 표현  ·  ⏱️ 5분            │
│  ███████░░░ 70%                    │
└─────────────────────────────────────┘
- 배경: surface (white)
- 그림자: sm
- borderRadius: 16px
- 패딩: 16px
- 호버/프레스: scale(0.98), 그림자 md

Expression Card (학습 중):
┌─────────────────────────────────────┐
│  💬 이걸 영어로?                    │
│                                     │
│  "라떼 한 잔 주세요"                 │
│                                     │
│  ─────────────────────────────────  │
│  [          입력창          ]       │
│  ─────────────────────────────────  │
│  💡 힌트: Can I get...             │
│  🔊 듣기                            │
└─────────────────────────────────────┘
- 배경: surface
- borderRadius: 20px
- 그림자: md
- 영어 텍스트: 20px, semibold
- 한국어: 16px, regular
```

#### 5.9.3 입력 필드 (Input)
```
Text Input:
┌─────────────────────────────────────┐
│  영어로 입력하세요...                │
└─────────────────────────────────────┘
- 배경: #F5F5F5
- 테두리: none (기본) / primary (포커스)
- borderRadius: 12px
- 높이: 52px
- 패딩: 16px
- 폰트: 18px (영어 입력용 크게)

Focused State:
- 테두리: primary 2px
- 배경: white
- 그림자: sm

Error State:
- 테두리: error 2px
- 배경: rgba(244,67,54,0.05)
```

#### 5.9.4 피드백 UI
```
정답 피드백:
┌─────────────────────────────────────┐
│  🎉 완벽해요!                        │
│                                     │
│  ✓ "Can I get a latte, please?"    │
│                                     │
│  💡 추가 팁:                         │
│  ─────────────────────────────────  │
│  "Can I get..."은 주문할 때 ...     │
└─────────────────────────────────────┘
- 배경: 그라데이션 (success → successLight)
- 상단 아이콘: 애니메이션 (confetti)
- 텍스트: white/dark

오답 피드백:
┌─────────────────────────────────────┐
│  😊 아깝다!                          │
│                                     │
│  ❌ 내 답: "Give me a latte"        │
│  ✅ 정답: "Can I get a latte?"      │
│                                     │
│  📊 72%가 이렇게 헷갈려요            │
│     (혼자가 아니에요!)               │
│                                     │
│  [다시] [다음으로]                   │
└─────────────────────────────────────┘
- 배경: surface
- 상단: 부드러운 코랄 (#FFAB91) 그라데이션
- 오답: incorrect 색상 (빨강 아닌 주황)
- 정답: success 색상
- "N%가 헷갈려요": textSecondary
```

#### 5.9.5 진행률 표시
```
Progress Bar:
███████░░░░░░░░ 47%
- 높이: 8px
- borderRadius: 4px
- 배경: #E0E0E0
- 채움: primary 그라데이션
- 애니메이션: 부드러운 증가

Circular Progress:
    ╭───╮
   ╱  70  ╲
  │   %    │
   ╲      ╱
    ╰───╯
- 크기: 80px
- 두께: 8px
- 배경: #E0E0E0
- 채움: primary → primaryLight 그라데이션
- 숫자: 24px, bold
```

### 5.10 애니메이션 & 트랜지션

#### 5.10.1 애니메이션 원칙
```yaml
Duration:
  instant: 100ms   # 호버, 프레스
  fast: 200ms      # 버튼, 토글
  normal: 300ms    # 카드 전환
  slow: 500ms      # 페이지 전환
  deliberate: 800ms # 축하 애니메이션

Easing:
  default: "ease-out"
  enter: "ease-out"     # 나타날 때
  exit: "ease-in"       # 사라질 때
  spring: "spring(1, 80, 10)"  # 탄성 효과

원칙:
  - 60fps 유지
  - 필수적인 곳만 애니메이션
  - 접근성: prefers-reduced-motion 존중
```

#### 5.10.2 주요 애니메이션
```yaml
카드 프레스:
  - scale: 1 → 0.98
  - duration: 100ms
  - spring 효과

화면 전환:
  - 슬라이드 (좌→우)
  - fade 동시
  - duration: 300ms

정답 축하:
  - confetti 파티클 (2초)
  - 카드 scale: 1 → 1.05 → 1
  - 체크마크 draw 애니메이션

오답:
  - 부드러운 shake (좌우 흔들림)
  - duration: 300ms
  - 강하지 않게 (부담 최소화)

스트릭 불꽃:
  - 펄스 애니메이션 (크기 변화)
  - 무한 반복
  - subtle하게

로딩:
  - 스피너 회전 (360도)
  - 스켈레톤 shimmer
```

#### 5.10.3 마이크로 인터랙션
```yaml
버튼 탭:
  - haptic feedback (light)
  - scale 0.95 → 1
  - duration: 100ms

토글 스위치:
  - thumb slide (spring)
  - 색상 전환 (200ms)
  - haptic feedback

Pull to Refresh:
  - 당기기 저항감
  - 로딩 스피너 전환
  - 완료 시 체크마크 → 사라짐

스와이프 제스처:
  - 카드 따라오기 (1:1)
  - 임계점 표시 (opacity 변화)
  - 완료 시 spring 되돌아가기
```

### 5.11 반응형 & 적응형 디자인

#### 5.11.1 화면 크기 대응
```yaml
Breakpoints:
  small: < 375px   # iPhone SE
  medium: 375-414px # iPhone 일반
  large: > 414px   # Plus/Max, 태블릿

Scaling Strategy:
  - 폰트: 시스템 폰트 크기 설정 존중
  - 간격: 기기 크기에 비례
  - 카드: 최대 너비 제한 (600px)

Small Screen 조정:
  - 카드 패딩: 16px → 12px
  - 버튼 높이: 48px → 44px
  - 타이틀: 24px → 20px
  - 아이콘: 24px → 20px

Tablet 조정:
  - 2컬럼 그리드 (시나리오 카드)
  - 최대 너비: 600px (중앙 정렬)
  - 여백 확대
```

#### 5.11.2 Safe Area 처리
```yaml
iOS:
  - notch 영역 피하기
  - home indicator 영역 피하기
  - 하단 버튼: safeAreaInsets.bottom + 16px

Android:
  - 상태바 영역
  - 네비게이션 바 영역
  - 펀치홀/노치 고려
```

### 5.12 편의성 기능

#### 5.12.1 빠른 접근
```yaml
홈에서 바로가기:
  - 마지막 학습 시나리오 "이어서 하기"
  - 오늘 복습할 표현 바로가기
  - 스트릭 상태 한눈에

제스처:
  - 우로 스와이프: 뒤로가기
  - 카드 스와이프: 다음/이전 표현
  - 길게 누르기: 표현 상세/저장

키보드:
  - 엔터: 정답 제출
  - 입력 중 자동 포커스
  - 완료 시 자동 다음으로
```

#### 5.12.2 자동 저장 & 복구
```yaml
자동 저장 (Auto-save):
  시점:
    - 표현 완료 직후
    - 세션 중 30초마다
    - 앱 백그라운드 진입 시
  내용:
    - 현재 세션 상태
    - 진행 중인 표현
    - 입력 중인 텍스트

복구 (Recovery):
  시나리오:
    - 앱 크래시 후 재실행
    - 배터리 방전 후
    - 강제 종료 후
  동작:
    - "이어서 할까요?" 모달
    - 마지막 상태 복원
    - 진행률 유지
```

#### 5.12.3 오프라인 상태 표시
```yaml
상태 표시:
┌─────────────────────────────────────┐
│  📴 오프라인 모드                    │
│  대부분의 기능을 사용할 수 있어요     │
└─────────────────────────────────────┘
- 위치: 화면 상단 (배너)
- 색상: warning 배경
- 자동 숨김: 온라인 복구 시

기능별 상태:
  ✅ 사용 가능:
    - 시나리오 학습
    - TTS 재생
    - 일기 작성
    - 설정 변경
  ⚠️ 제한적:
    - AI 회화 (불가)
    - 동기화 (대기열)
```

#### 5.12.4 데이터 동기화
```yaml
백업 트리거:
  - 수동: 설정 > 백업하기
  - 자동: 일주일마다 (설정에서 on/off)

백업 내용:
  - 학습 진행률
  - 스킬 상태
  - 설정
  - 일기 (선택)

복원:
  - 파일 선택 복원
  - 클라우드 복원 (향후)
```

#### 5.12.5 입력 편의성
```yaml
영어 입력 보조:
  자동 대문자:
    - 문장 시작 시 자동 대문자
    - 비활성화 옵션 제공

  자동 완성 제안:
    - 자주 쓰는 표현 제안
    - 배운 표현 우선 표시

  맞춤법 검사:
    - 빨간 밑줄 대신 부드러운 표시
    - 제출 전 확인 (선택적)

입력 필드:
  - 자동 포커스
  - 키보드 위 "완료" 버튼
  - 입력 취소 버튼 (X)
  - 전체 지우기

음성 입력 (향후):
  - 마이크 버튼
  - 음성 → 텍스트 변환
  - 발음 비교 (선택)
```

#### 5.12.6 학습 편의 기능
```yaml
힌트 시스템:
  1단계: 첫 글자 (C...)
  2단계: 단어 개수 (_ _ _ _ _)
  3단계: 전체 힌트 (Can I get...)
  별 소모: 단계별 5/10/20⭐

오디오 컨트롤:
  - 자동 재생 (on/off)
  - 반복 재생 버튼
  - 속도 조절 (0.5x ~ 1.5x)
  - 구간 반복 (향후)

북마크:
  - 표현 저장하기 ⭐
  - 저장한 표현 목록
  - 복습 모드에서 우선 표시

메모:
  - 표현별 메모 추가
  - "이거 헷갈림" 태그
  - 복습 시 메모 표시
```

#### 5.12.7 접근성 강화
```yaml
시각:
  - 폰트 크기 4단계
  - 고대비 모드
  - 색맹 모드 (향후)

청각:
  - TTS 속도 조절
  - 자막 표시 (음성 콘텐츠)
  - 진동 피드백

운동:
  - 큰 터치 영역 (44px+)
  - 제스처 대안 (버튼)
  - 한 손 조작 최적화

인지:
  - 단순한 UI
  - 일관된 패턴
  - 명확한 피드백
  - 실수 되돌리기

스크린 리더:
  - 모든 요소 레이블
  - 의미 있는 순서
  - 상태 변화 알림
```

#### 5.12.8 성능 최적화 (사용자 체감)
```yaml
로딩 최적화:
  - 스켈레톤 UI (빈 화면 방지)
  - 점진적 로딩
  - 우선순위 로딩 (보이는 것 먼저)

캐싱:
  - 시나리오 데이터 캐싱
  - TTS 오디오 캐싱
  - 이미지 캐싱

메모리:
  - 리스트 가상화 (FlatList)
  - 이미지 최적화
  - 불필요한 리렌더 방지

배터리:
  - 백그라운드 작업 최소화
  - 애니메이션 최적화
  - 네트워크 요청 배칭
```

---

## Part 6: UX 상세

### 6.1 사용자 여정

#### 6.1.1 Day 1: 첫 경험
```
1. 앱 설치 → 온보딩 시작
   ├── 목적 선택 (여행/일/이민/공부)
   └── 난이도 선택 (처음/조금/중급)

2. 홈 화면 도착
   ├── 추천 시나리오 표시 (목적 기반)
   └── "첫 학습 해볼까요?" CTA

3. 첫 학습 (30초 모드 추천)
   ├── 가장 쉬운 표현 1개
   ├── 성공 → 🎉 "첫 문장 완성!"
   └── 실패 → 😊 "괜찮아요, 다시 해볼래요?"

4. 완료 후
   ├── "1개 배웠어요!" 축하
   ├── 🔥 1일차 스트릭 시작
   └── 일기 연결 유도 (선택)

목표: 5분 안에 첫 성공 경험
```

#### 6.1.2 Day 7: 습관 형성
```
1. 앱 열기 (푸시 알림 통해)
   └── "오늘도 왔군요! 🔥 7일 연속!"

2. 학습 패턴 형성
   ├── 평균 세션 시간 파악
   ├── 선호 시나리오 분석
   └── 개인화 추천 시작

3. 약점 발견
   ├── "관사(a/the) 연습해볼까요?"
   └── 약점 타겟 학습 제안

4. 보상 강화
   ├── "7일 연속 배지" 획득
   └── 상점에서 테마 해금

목표: 일주일 습관 형성 (리텐션 핵심)
```

#### 6.1.3 Day 30: 성장 확인
```
1. 월간 리뷰
   ├── 배운 표현 총 개수
   ├── 마스터한 시나리오
   └── 개선된 스킬 그래프

2. 레벨 업 제안
   └── "이제 다음 레벨 해볼까요?"

3. 새 시나리오 추천
   ├── 완료한 시나리오 기반
   └── 관련 시나리오 제안

4. 커뮤니티 유도
   └── "내 학습 기록 공유하기"

목표: 성장 시각화 → 장기 리텐션
```

#### 6.1.4 이탈 → 복귀
```
3일 미접속:
├── 푸시: "🥺 보고 싶었어요"
└── 복귀 시: "다시 오셨군요! 30초만 해볼까요?"

7일 미접속:
├── 푸시: "스트릭 리셋 전에 돌아오세요!"
└── 복귀 시: 스트릭 복구 기회 1회 제공

30일 미접속:
├── 이메일: "영어 학습, 다시 시작해볼까요?"
└── 복귀 시: 새 시작 제안 + 보너스 별 지급
```

### 6.2 알림 전략

#### 6.2.1 알림 유형
```yaml
학습 리마인더:
  시간: 사용자 설정 (기본 저녁 8시)
  내용: "오늘 영어 공부 했나요? 30초면 돼요 🔥"
  빈도: 1일 1회

스트릭 위험:
  조건: 오늘 학습 안 하고 저녁 10시
  내용: "🔥 스트릭 끊기기 2시간 전! 30초만 해요"
  빈도: 1일 1회 (학습 안 한 날만)

복습 알림:
  조건: SRS 복습 필요 항목 5개 이상
  내용: "복습할 표현이 쌓였어요! (5개)"
  빈도: 2-3일마다

성취 알림:
  조건: 배지 획득, 레벨업
  내용: "🎉 7일 연속 달성! 배지 획득"
  빈도: 이벤트 시
```

#### 6.2.2 알림 설정 UI
```
┌─────────────────────────────────────┐
│  🔔 알림 설정                        │
├─────────────────────────────────────┤
│                                     │
│  📚 학습 리마인더                    │
│  [ON] ─────○                        │
│  시간: [20:00 ▼]                    │
│                                     │
│  🔥 스트릭 위험 알림                 │
│  [ON] ─────○                        │
│  "스트릭 끊기기 전 알려줘요"          │
│                                     │
│  📝 복습 알림                        │
│  [ON] ─────○                        │
│  "복습할 표현이 쌓이면 알려줘요"       │
│                                     │
│  🎉 성취 알림                        │
│  [ON] ─────○                        │
│  "배지 획득, 레벨업 시 알려줘요"       │
│                                     │
└─────────────────────────────────────┘
```

#### 6.2.3 알림 빈도 제한
```
원칙:
- 하루 최대 2개 알림
- 방해금지 시간 존중 (23:00-07:00)
- 학습 완료한 날은 리마인더 안 보냄
- 3회 연속 무시 시 빈도 감소
```

### 6.3 접근성

#### 6.3.1 폰트 크기 조절
```
┌─────────────────────────────────────┐
│  🔤 글자 크기                        │
├─────────────────────────────────────┤
│                                     │
│  [소]   [중]   [대]   [매우 큰]      │
│         ◉                          │
│                                     │
│  미리보기:                           │
│  ┌──────────────────────────────┐  │
│  │ Can I get a latte?           │  │
│  │ 라떼 한 잔 주세요              │  │
│  └──────────────────────────────┘  │
│                                     │
└─────────────────────────────────────┘

크기별 스케일:
- 소: 0.85x (14px 기본)
- 중: 1.0x (16px 기본) ← 기본값
- 대: 1.15x (18px 기본)
- 매우 큰: 1.3x (21px 기본)
```

#### 6.3.2 TTS 속도 조절
```
┌─────────────────────────────────────┐
│  🔊 듣기 속도                        │
├─────────────────────────────────────┤
│                                     │
│  느림 ────────●────────── 빠름      │
│              0.8x                   │
│                                     │
│  [▶️ 미리 듣기]                      │
│                                     │
└─────────────────────────────────────┘

속도 옵션:
- 0.5x: 아주 느림 (초보자용)
- 0.8x: 느림 ← 기본값
- 1.0x: 보통
- 1.2x: 빠름
- 1.5x: 아주 빠름 (고급자용)
```

#### 6.3.3 고대비 모드
```
일반 모드:
├── 배경: #FFFFFF
├── 텍스트: #333333
└── 강조: #2196F3

고대비 모드:
├── 배경: #000000
├── 텍스트: #FFFFFF
└── 강조: #FFFF00
```

### 6.4 설정 화면

#### 6.4.1 전체 설정 구조
```
┌─────────────────────────────────────┐
│  ⚙️ 설정                             │
├─────────────────────────────────────┤
│                                     │
│  📚 학습                             │
│  ├── 학습 목표 변경 (여행/일/이민)    │
│  ├── 일일 목표량 (1개/3개/5개)       │
│  └── 난이도 조절                     │
│                                     │
│  🔔 알림                             │
│  ├── 학습 리마인더                   │
│  ├── 스트릭 알림                     │
│  └── 복습 알림                       │
│                                     │
│  🎨 화면                             │
│  ├── 다크 모드                       │
│  ├── 글자 크기                       │
│  └── 고대비 모드                     │
│                                     │
│  🔊 소리                             │
│  ├── 효과음 (ON/OFF)                 │
│  ├── TTS 속도                        │
│  └── 배경음악 (ON/OFF)               │
│                                     │
│  📦 데이터                           │
│  ├── 백업하기                        │
│  ├── 복원하기                        │
│  └── 데이터 초기화                   │
│                                     │
│  ℹ️ 정보                             │
│  ├── 버전 정보                       │
│  ├── 개인정보처리방침                 │
│  └── 이용약관                        │
│                                     │
└─────────────────────────────────────┘
```

### 6.5 에러 처리

#### 6.5.1 콘텐츠 완료 시
```
시나리오 모두 완료:
┌─────────────────────────────────────┐
│  🎉 카페 시나리오 마스터!             │
├─────────────────────────────────────┤
│                                     │
│  모든 표현을 완료했어요!              │
│                                     │
│  📊 성적:                            │
│  └── 10개 표현 마스터                │
│                                     │
│  다음에 뭐 해볼까요?                  │
│  ┌──────────────────────────────┐  │
│  │ 🍽️ 식당 주문하기              │  │
│  │ (비슷한 주제)                 │  │
│  └──────────────────────────────┘  │
│  ┌──────────────────────────────┐  │
│  │ 🔄 복습 모드                  │  │
│  │ (배운 표현 다시 연습)         │  │
│  └──────────────────────────────┘  │
│                                     │
└─────────────────────────────────────┘
```

#### 6.5.2 계속 실패 시
```
3회 연속 실패:
┌─────────────────────────────────────┐
│  💪 조금 어려워 보이네요              │
├─────────────────────────────────────┤
│                                     │
│  이 표현은 나중에 다시 해볼까요?       │
│                                     │
│  [힌트 더 보기] [쉬운 것부터] [건너뛰기]│
│                                     │
└─────────────────────────────────────┘

5회 연속 실패 (세션 전체):
┌─────────────────────────────────────┐
│  🌱 오늘은 여기까지 할까요?           │
├─────────────────────────────────────┤
│                                     │
│  내일 더 쉬운 표현으로 다시 시작해요!  │
│                                     │
│  💡 팁: 난이도를 "처음이에요"로        │
│     바꾸면 더 쉬운 표현부터 해요       │
│                                     │
│  [설정에서 난이도 변경]               │
│                                     │
└─────────────────────────────────────┘
```

#### 6.5.3 앱 크래시 복구
```typescript
// 세션 복구 로직
export async function recoverSession() {
  // 1. 마지막 세션 확인
  const lastSession = await AsyncStorage.getItem('current_session');

  if (lastSession && !lastSession.isCompleted) {
    // 2. 복구 모달 표시
    showRecoveryModal({
      title: '이어서 할까요?',
      message: `${lastSession.scenarioName} 학습 중이었어요`,
      options: [
        { label: '이어서 하기', action: 'resume' },
        { label: '새로 시작', action: 'restart' }
      ]
    });
  }
}
```

#### 6.5.4 네트워크 에러 (AI 회화)
```
┌─────────────────────────────────────┐
│  📡 인터넷 연결이 필요해요            │
├─────────────────────────────────────┤
│                                     │
│  AI 회화는 인터넷이 필요해요.         │
│                                     │
│  대신 이건 어때요?                    │
│  ┌──────────────────────────────┐  │
│  │ 📚 시나리오 연습 (오프라인)    │  │
│  │ "인터넷 없이 할 수 있어요"     │  │
│  └──────────────────────────────┘  │
│                                     │
│  [다시 시도] [시나리오 연습하기]       │
│                                     │
└─────────────────────────────────────┘
```

---

## Part 7: 비즈니스

### 7.1 수익 모델

#### 7.1.1 무료 vs 프리미엄
```yaml
무료 (핵심 기능):
  ✅ 모든 시나리오 학습 (오프라인)
  ✅ 30초/1분/5분 세션
  ✅ 진도 트래킹
  ✅ SRS 복습
  ✅ 일기 작성
  ✅ 스트릭/배지
  ✅ TTS 음성
  ✅ 광고 포함 (학습 완료 후)

  🔒 AI 회화: 일 5회 무료

프리미엄 ($2.99-$4.99/월):
  ✅ 무료 기능 전체
  ✅ 광고 제거
  ✅ AI 회화 무제한
  ✅ 프리미엄 시나리오 (비즈니스 고급)
  ✅ 상세 분석 리포트
  ✅ 우선 고객 지원
```

#### 7.1.2 광고 전략
```yaml
배너 광고:
  위치: 홈 화면 하단
  타입: 작은 배너 (320x50)
  빈도: 상시 (학습 화면 제외)

인터스티셜:
  위치: 세션 완료 후
  빈도: 3세션마다 1회
  스킵: 5초 후 스킵 가능

보상형 광고:
  위치: 상점, AI 추가 사용
  보상:
    - 광고 시청 → ⭐ 10개
    - 광고 시청 → AI 회화 +2회
  빈도: 사용자 선택
```

#### 7.1.3 예상 수익
```yaml
가정:
  MAU: 10,000명
  프리미엄 전환: 3% (300명)
  광고 eCPM: $1.5

월 수익:
  프리미엄: 300 × $3.99 = $1,197
  배너: 10,000 × 30일 × 5노출 × $0.001 = $1,500
  인터스티셜: 10,000 × 10세션 × $0.01 = $1,000
  보상형: 10,000 × 2회 × $0.02 = $400
  ─────────────────────────────────
  총: ~$4,000/월

성장 시 (MAU 100,000):
  프리미엄: 3,000 × $3.99 = $11,970
  광고: ~$25,000
  ─────────────────────────────────
  총: ~$40,000/월
```

### 7.2 경쟁사 분석

#### 7.2.1 글로벌 앱
```yaml
Duolingo:
  장점:
    - 브랜드 인지도
    - 게이미피케이션 최고
    - 다양한 언어
  단점:
    - 맥락 없는 문장
    - 한국인 특화 부족
    - 온라인 필수
  WhatTodo 기회:
    → 시나리오 기반으로 차별화
    → 한국어 문법 설명
    → 오프라인 지원

Cake:
  장점:
    - 영상 기반 학습
    - 자연스러운 표현
  단점:
    - 수동적 학습
    - 체계적 커리큘럼 부족
  WhatTodo 기회:
    → 능동적 학습 + 체계적 진행

Speak:
  장점:
    - AI 회화 특화
    - 발음 교정
  단점:
    - 고가 (월 $9.99+)
    - 온라인 필수
  WhatTodo 기회:
    → 저렴한 대안 + 오프라인
```

#### 7.2.2 국내 앱
```yaml
야나두:
  장점:
    - 한국인 타겟 콘텐츠
    - 강의 품질
  단점:
    - 강의 형태 (수동적)
    - 고가
  WhatTodo 기회:
    → 앱 기반 능동 학습

시원스쿨:
  장점:
    - 초보자 친화적
    - 한국어 설명
  단점:
    - 강의 중심
    - 앱 UX 부족
  WhatTodo 기회:
    → 현대적 앱 UX + 게이미피케이션
```

#### 7.2.3 포지셔닝
```
                 게이미피케이션
                      ↑
                      │
    Duolingo ●        │        ● WhatTodo (목표)
                      │
시스템적 ─────────────┼─────────────→ 시나리오 기반
                      │
                      │        ● Cake
    야나두 ●          │
                      │
                      ↓
                   교육적

핵심 차별점:
"시나리오 기반 + 게이미피케이션 + 오프라인 + 한국인 특화"
```

### 7.3 KPIs

#### 7.3.1 성장 지표
```yaml
다운로드:
  목표: 월 1,000+ 신규 설치
  측정: App Store Connect, Google Play Console

MAU (Monthly Active Users):
  목표: 10,000+ (6개월 후)
  측정: Firebase Analytics

DAU/MAU:
  목표: 30%+ (Duolingo 수준)
  측정: Firebase Analytics
```

#### 7.3.2 리텐션 지표
```yaml
Day 1 Retention:
  목표: 40%+
  측정: 설치 다음날 앱 열기

Day 7 Retention:
  목표: 20%+
  측정: 설치 7일 후 앱 열기

Day 30 Retention:
  목표: 10%+
  측정: 설치 30일 후 앱 열기

스트릭 유지율:
  목표: 7일 스트릭 달성 20%+
  측정: 스트릭 이벤트 추적
```

#### 7.3.3 수익 지표
```yaml
ARPU (Average Revenue Per User):
  목표: $0.50/월+
  측정: 총 수익 / MAU

프리미엄 전환율:
  목표: 3%+
  측정: 프리미엄 사용자 / MAU

LTV (Lifetime Value):
  목표: $5.00+
  측정: 사용자당 총 수익
```

#### 7.3.4 학습 지표
```yaml
세션당 학습량:
  목표: 5개+ 표현
  측정: 세션 완료 이벤트

일일 학습 완료율:
  목표: 60%+ (앱 연 사용자 중)
  측정: 세션 완료 이벤트

시나리오 완료율:
  목표: 30%+ 사용자가 1개 이상 완료
  측정: 시나리오 완료 이벤트
```

### 7.4 리스크

#### 7.4.1 기술 리스크
```yaml
오프라인 데이터 손실:
  위험도: 중
  대응:
    - 자동 백업 기능
    - 클라우드 백업 옵션 (프리미엄)
    - 데이터 복구 가이드

API 비용 증가:
  위험도: 중
  대응:
    - 일일 무료 제한 (5회)
    - 응답 캐싱
    - 사용량 모니터링
    - 대안 모델 준비 (gpt-3.5 → 오픈소스)
```

#### 7.4.2 사업 리스크
```yaml
경쟁사 진입:
  위험도: 높음
  대응:
    - 한국인 특화 콘텐츠 강화
    - 커뮤니티 구축 (락인 효과)
    - 빠른 기능 개선

사용자 이탈:
  위험도: 높음
  대응:
    - 리텐션 기능 강화
    - 푸시 알림 최적화
    - 개인화 추천 개선
```

#### 7.4.3 콘텐츠 리스크
```yaml
콘텐츠 품질 문제:
  위험도: 중
  대응:
    - 네이티브 검수
    - 사용자 피드백 수집
    - A/B 테스트

콘텐츠 부족:
  위험도: 중
  대응:
    - GPT 기반 콘텐츠 생성
    - 사용자 생성 콘텐츠 (향후)
    - 정기적 콘텐츠 추가
```

---

## Part 8: 출시 & 운영

### 8.1 출시 계획

#### 8.1.1 Phase 1: 소프트 런칭
```yaml
기간: 1-2개월
대상: 한국 베타 테스터 (100-500명)
목표:
  - 핵심 버그 발견
  - UX 피드백 수집
  - 리텐션 데이터 확보

채널:
  - 개인 네트워크
  - 영어 학습 커뮤니티
  - 오픈 카카오톡 방

수집:
  - Firebase Crashlytics
  - 인앱 피드백 버튼
  - 주간 사용자 인터뷰
```

#### 8.1.2 Phase 2: 정식 출시
```yaml
기간: 소프트 런칭 후
대상: 한국 전체
채널:
  - App Store / Google Play
  - ASO 최적화
  - 앱 리뷰 요청

ASO 키워드:
  메인: 영어 학습, 영어 공부, 영어 회화
  서브: 영어 앱, 영어 독학, 무료 영어
  롱테일: 영어 초보, 영어 여행, 직장인 영어
```

#### 8.1.3 Phase 3: 성장
```yaml
기간: 정식 출시 3개월 후
목표: MAU 10,000 달성
채널:
  - 인스타그램/유튜브 마케팅
  - 인플루언서 협업
  - 커뮤니티 마케팅
```

### 8.2 마케팅

#### 8.2.1 초기 사용자 확보
```yaml
전략 1: 커뮤니티 마케팅
  채널:
    - 네이버 카페 (영어 학습)
    - 오픈 카카오톡
    - 에브리타임 (대학생)
  방법:
    - 앱 소개 + 무료 제공
    - 베타 테스터 모집
    - 피드백 이벤트

전략 2: 콘텐츠 마케팅
  채널:
    - 블로그 (영어 학습 팁)
    - 인스타그램 (영어 표현)
    - 유튜브 (학습법)
  방법:
    - 유용한 콘텐츠 → 앱 유도
    - SEO 최적화

전략 3: 레퍼럴
  방법:
    - 친구 초대 시 별 보상
    - 공유하기 기능
    - 초대 코드 시스템
```

#### 8.2.2 마케팅 메시지
```
메인 메시지:
"30초만 있어도 영어가 늘어요"

서브 메시지:
- "영포자도 할 수 있는 영어 앱"
- "틀려도 창피하지 않아요"
- "인터넷 없이도 공부해요"
- "진짜 쓰는 영어만 배워요"

타겟별 메시지:
- 영포자: "영어, 다시 시작해볼까요?"
- 직장인: "출퇴근 시간에 영어 공부"
- 여행자: "여행 가기 전 딱 필요한 표현"
```

### 8.3 콘텐츠 운영

#### 8.3.1 콘텐츠 추가 계획
```yaml
초기 (출시 시):
  - 20개 시나리오
  - 200개 표현
  - 여행/일상 중심

3개월 후:
  - +10개 시나리오
  - 비즈니스 시나리오 추가
  - 고급 표현 추가

6개월 후:
  - +10개 시나리오
  - 사용자 요청 시나리오
  - 시즌/이벤트 콘텐츠

추가 주기: 월 2-3개 시나리오
```

#### 8.3.2 품질 관리
```yaml
검수 프로세스:
  1. 콘텐츠 작성 (내부/GPT)
  2. 네이티브 검수
  3. 테스터 피드백
  4. 수정 및 배포

품질 기준:
  - 실제 사용되는 표현만
  - 발음/억양 자연스러움
  - 문법 설명 정확성
  - 한국어 번역 자연스러움

피드백 수집:
  - 인앱 신고 버튼
  - 표현별 좋아요/싫어요
  - 월간 사용자 설문
```

### 8.4 법적 요구사항

#### 8.4.1 개인정보처리방침
```yaml
수집 항목:
  필수:
    - 기기 ID (익명)
    - 학습 데이터 (로컬)
  선택:
    - 이메일 (백업용)
    - 푸시 토큰

수집 목적:
  - 서비스 제공
  - 학습 진도 저장
  - 알림 발송

보관 기간:
  - 서비스 이용 중
  - 탈퇴 시 즉시 삭제

제3자 제공:
  - Firebase (분석/크래시)
  - AdMob (광고)
```

#### 8.4.2 이용약관
```yaml
핵심 조항:
  - 서비스 이용 범위
  - 유료 서비스 결제/환불
  - 콘텐츠 저작권
  - 면책 조항

연령 제한:
  - 13세 이상 (COPPA 준수)
  - 부모 동의 필요 시 명시
```

### 8.5 테스트 전략

#### 8.5.1 단위 테스트
```yaml
범위:
  - Store 로직 (Zustand)
  - 유틸리티 함수
  - SRS 알고리즘
  - 데이터 변환

커버리지 목표: 80%+

도구: Jest
```

#### 8.5.2 E2E 테스트
```yaml
시나리오:
  - 온보딩 완료
  - 첫 학습 세션 완료
  - 정답/오답 흐름
  - 세션 중단/복구
  - 설정 변경
  - 백업/복원

도구: Detox 또는 Maestro
```

#### 8.5.3 QA 체크리스트
```yaml
기능 테스트:
  - [ ] 온보딩 완료 흐름
  - [ ] 시나리오 선택 및 학습
  - [ ] 30초/1분/5분 세션
  - [ ] 정답/오답 피드백
  - [ ] 스트릭 업데이트
  - [ ] 일기 작성
  - [ ] AI 회화 (온라인)
  - [ ] 설정 변경 저장

호환성 테스트:
  - [ ] iOS 15+
  - [ ] Android 10+
  - [ ] 다양한 화면 크기
  - [ ] 오프라인 모드

성능 테스트:
  - [ ] 앱 시작 시간 <3초
  - [ ] 화면 전환 <1초
  - [ ] 메모리 사용량 <150MB
```

### 8.6 성능 요구사항

#### 8.6.1 앱 성능
```yaml
시작 시간:
  콜드 스타트: <3초
  웜 스타트: <1초

화면 전환:
  일반: <500ms
  데이터 로드: <1초

메모리:
  평균: <100MB
  최대: <200MB

배터리:
  1시간 사용: <5% 소모
```

#### 8.6.2 오프라인 성능
```yaml
로컬 저장소:
  시나리오 데이터: ~5MB
  사용자 데이터: ~1MB
  총 앱 크기: <50MB

동기화:
  백업 데이터: <1MB
  복원 시간: <5초
```

#### 8.6.3 AI 성능 (온라인)
```yaml
응답 시간:
  첫 메시지: <3초
  후속 메시지: <2초

에러 처리:
  타임아웃: 10초
  재시도: 자동 1회
```

---

## 부록: 요약

### A. 핵심 원칙 재확인
```
1. 오프라인 우선 - AI만 온라인
2. 시나리오 기반 - Week 구조 없음
3. 자유로운 학습 - 강제 순서 없음
4. 영포자 친화 - 30초도 성공
```

### B. 주요 수정 파일
```
신규:
- store/scenarioStore.ts
- store/skillStore.ts
- components/scenarios/*
- components/session/*
- data/scenarios/**

수정:
- app/(tabs)/index.tsx
- components/home/TodayScreen.tsx
- store/learnStore.ts
```

### C. 우선순위
```
Phase 1: 시나리오 구조 전환
Phase 2: 세션 시스템 (30초/1분/5분)
Phase 3: UI/UX 개편
Phase 4: 테스트 및 출시
```

---

> **문서 버전**: 2.0
> **작성 완료**: 2025-12-29
> **다음 업데이트**: 구현 시작 후
